[lib]
name = "core_embed_binaries"

[package]
name = "core_embed_binaries"
version = "0.1.0"
edition = "2021"
authors = ["Markus Moenig"]

[dependencies]
rust-embed = { version = "8.0.0", default-features = true, features = ["include-exclude"]}
